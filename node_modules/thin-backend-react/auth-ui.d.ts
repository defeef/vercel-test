import React from 'react';
export declare function LoginAndSignUp(): JSX.Element;
interface LoginProps {
    description?: string;
    onSignUpClick: () => void;
    /**
     * An image shown above the title in the login form.
     *
     * If nothing is specified, the Thin logo is used as the default icon.
     *
     * @example
     * <Login appIcon={<img src="/custom-icon.png" />} />
     */
    appIcon?: React.ReactNode;
    /**
     * Loading spinner shown while the login form is loading.
     *
     * @example
     * <Login loadingSpinner={<CustomLoadingSpinner />} />
     */
    loadingSpinner?: React.ReactNode;
}
export declare function Login({ description, onSignUpClick, appIcon, loadingSpinner }: LoginProps): JSX.Element;
interface SignUpProps {
    description?: string;
    onLoginClick: () => void;
    /**
     * An image shown above the title in the signup form.
     *
     * If nothing is specified, the Thin logo is used as the default icon.
     *
     * @example
     * <SignUp appIcon={<img src="/custom-icon.png" />} />
     */
    appIcon?: React.ReactNode;
    /**
     * Loading spinner shown while the sign up form is loading.
     *
     * @example
     * <SignUp loadingSpinner={<CustomLoadingSpinner />} />
     */
    loadingSpinner?: React.ReactNode;
}
export declare function SignUp({ description, onLoginClick, appIcon, loadingSpinner }: SignUpProps): JSX.Element;
export declare function EmailConfirmation({ userId, token, onConfirmedAlready, onConfirmFailed }: {
    userId: any;
    token: any;
    onConfirmedAlready: any;
    onConfirmFailed: any;
}): JSX.Element;
export {};
